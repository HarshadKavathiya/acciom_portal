tags:
- "TestSuite"
summary: "Find testsuite by userID"
description: "Returns a testsuite"
operationId: "gettestsuite"
produces:
- "application/json"
parameters:
- name: "user_id"
  in: "path"
  description: "UserID of testsuite to return"
  required: true
  type: "integer"
  format: "int64"
responses:
  200:
    description: "successful operation"
  400:
    description: "Invalid ID supplied"
  404:
    description: "suite not found"
