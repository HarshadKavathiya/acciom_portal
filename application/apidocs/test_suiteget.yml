tags:
  - "TestSuite"
summary: "Find testsuite by userID"
description: "Returns a testsuite"
operationId: "gettestsuite"
produces:
  - "application/json"
parameters:
  - name: "Authorization"
    in: "header"
    description: "Bearer ey....."
    required: true
    type: "string"
responses:
  200:
    description: "successful operation"
  400:
    description: "Invalid ID supplied"
  404:
    description: "suite not found"
