tags:
- "User"
summary: "verify account"
description: "Returns a true user"
operationId: "verifyuser"
produces:
- "application/json"
parameters:
- name: "token"
  in: "path"
  description: "enter token"
  required: true
  type: "string"
responses:
  200:
    description: "successful operation"
  400:
    description: "Invalid ID supplied"
  404:
    description: "Pet not found"
